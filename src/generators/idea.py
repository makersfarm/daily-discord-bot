def build_public_data_prompt(data: dict) -> str:
    datasets = "\n".join(
        f"- {d['title']} ({d['org']}): {d['desc'][:80]} [키워드: {d['keywords'][:50]}]"
        for d in data["datasets"]
    )
    return f"""오늘의 공공데이터 아이디어를 만들어줘.

분야: {data['category']}
오늘 선택된 공공데이터 API:
{datasets}

먼저 이 데이터를 활용하는 기존 유사 서비스들을 파악하고, 그 문제점을 짚어줘.
그 다음 아이디어를 각 유형별로 3개씩 제안해줘.
추가 질문이나 부연 설명(웹 검색 권한, Sources 등) 없이 바로 형식대로만 출력해.
아이디어 번호 앞 이모지는 💡 만 사용해.

형식:
🔍 유사 서비스 현황 (2~3개) — 국내 서비스 위주. 형식: [서비스명](실제 접근 가능한 URL) — 한 줄 설명
⚠️ 기존 서비스의 문제점 (2~3가지 bullet)

[아이디어 A — 기존 문제점 개선] 3개

**1. 💡 아이디어명**
목적: 누구를 위해 어떤 문제를 푸는지
기능
• 핵심 기능
• 핵심 기능

**2. 💡 아이디어명**
목적: 누구를 위해 어떤 문제를 푸는지
기능
• 핵심 기능
• 핵심 기능

**3. 💡 아이디어명**
목적: 누구를 위해 어떤 문제를 푸는지
기능
• 핵심 기능
• 핵심 기능

[아이디어 B — 기존 서비스와 겹치지 않는 새로운 방향] 3개

**1. 💡 아이디어명**
목적: 누구를 위해 어떤 문제를 푸는지
기능
• 핵심 기능
• 핵심 기능

**2. 💡 아이디어명**
목적: 누구를 위해 어떤 문제를 푸는지
기능
• 핵심 기능
• 핵심 기능

**3. 💡 아이디어명**
목적: 누구를 위해 어떤 문제를 푸는지
기능
• 핵심 기능
• 핵심 기능
"""


def build_aihub_prompt(data: dict) -> str:
    tags = ", ".join(data["tags"]) if data["tags"] else "없음"
    desc_line = f"설명: {data['desc']}\n" if data.get("desc") else ""
    return f"""오늘의 AI Hub 데이터셋 아이디어를 만들어줘.

데이터셋명: {data['title']}
분야: {data['field']}
데이터 유형: {data['type']}
태그: {tags}
{desc_line}구축년도: {data['year']}
링크: {data['url']}

먼저 이 데이터셋과 유사한 AI 서비스들을 파악하고, 그 문제점을 짚어줘.
그 다음 아이디어를 각 유형별로 3개씩 제안해줘.
추가 질문이나 부연 설명(웹 검색 권한, Sources 등) 없이 바로 형식대로만 출력해.
아이디어 번호 앞 이모지는 💡 만 사용해.

형식:
🔍 유사 서비스 현황 (2~3개) — 국내 서비스 위주. 형식: [서비스명](실제 접근 가능한 URL) — 한 줄 설명
⚠️ 기존 서비스의 문제점 (2~3가지 bullet)

[아이디어 A — 기존 문제점 개선] 3개

**1. 💡 아이디어명**
목적: 누구를 위해 어떤 문제를 푸는지
기능
• 핵심 기능
• 핵심 기능

**2. 💡 아이디어명**
목적: 누구를 위해 어떤 문제를 푸는지
기능
• 핵심 기능
• 핵심 기능

**3. 💡 아이디어명**
목적: 누구를 위해 어떤 문제를 푸는지
기능
• 핵심 기능
• 핵심 기능

[아이디어 B — 기존 서비스와 겹치지 않는 새로운 방향] 3개

**1. 💡 아이디어명**
목적: 누구를 위해 어떤 문제를 푸는지
기능
• 핵심 기능
• 핵심 기능

**2. 💡 아이디어명**
목적: 누구를 위해 어떤 문제를 푸는지
기능
• 핵심 기능
• 핵심 기능

**3. 💡 아이디어명**
목적: 누구를 위해 어떤 문제를 푸는지
기능
• 핵심 기능
• 핵심 기능
"""
